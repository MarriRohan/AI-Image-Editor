apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: duality-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: duality-api
                port:
                  number: 80
